1️⃣ What is the difference between var, let, and const?

answer: var have many issues like it can be declare same name in multiple times and can be called or used before define, var is hoisted but value not so it make confusions, it's call functional scope, let and const has been solved the problems of var let and const 2 are block scoped and both only can assign one name, let value can be reassign later while const value can't be re assign later etc. they are the some differences between them.in mordern js var is totally not useable today, while let and const are the mostly used keyword for varriable if value need to reassign later we use let rather than that we mostly use const.


2️⃣ What is the spread operator (...)?
ans: it is an feature that helps us to spread array , string value without using loop. example:
 const arr = [1,2,3];
 const newArr = [...arr]//[1,2,3]


3️⃣ What is the difference between map(), filter(), and forEach()?
answer: those 3 are powerfull array method, main difference between map(),filter and forEach method is 
map()=> loop every element return full new array,
filter()=> loop every element return an array contain only specific value allow the condition;
forEach()=> only loop every element, don't return any thing;

4️⃣ What is an arrow function?
ans: arrow function is ES6 new function writing technique without using function keyword;
for example: const arrowFunc = (arg)=>{},

5️⃣ What are template literals?
answer: wrting string via using `` this symple which allow us to write multiline string, writing js inside string etc.


since I didn't use any ai chat bot or grammer checker that's why there maybe I made some gramatical mistake. please consider this issue. thank You!



